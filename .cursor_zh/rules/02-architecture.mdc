---
description: 项目架构规则
alwaysApply: true
---

# 架构优先

实现前：

1. 识别受影响的层次
2. 确保关注点分离
3. 遵循既有架构

# 项目架构

保持层次清晰、职责单一。若设计不清，先提出架构再写代码。

遵循以下架构原则。

前端目录结构：

- components/
- pages/
- hooks/
- services/
- utils/
- types/

后端目录结构：

- controllers/
- services/
- repositories/
- models/
- utils/

规则：

1. 业务逻辑必须放在服务层。
2. 控制器只处理请求/响应。
3. 避免在 React 组件中堆砌业务逻辑。
4. 工具函数必须无状态。

不要混层。

若设计不清：

先提出架构再编码。
