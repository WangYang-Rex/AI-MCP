---
description: Node 后端 API 规则
globs: ["**/*.ts"]
---

# 后端 API 规则

遵循分层架构。

控制器职责：

- 接收请求
- 校验输入
- 调用服务层
- 返回响应

服务层职责：

- 业务逻辑
- 数据编排

仓储层职责：

- 仅负责数据库查询

API 规则：

- 使用 async/await
- 不要暴露原始数据库错误
- 始终校验输入
